// Utility function to get client information\nexport const getClientInfo = () => {\n  return {\n    user_agent: navigator.userAgent,\n    ip_address: '127.0.0.1', // Browser can't access real IP directly\n    platform: navigator.platform,\n    language: navigator.language,\n    timestamp: new Date().toISOString()\n  };\n};\n\n// Alternative method to try to get more accurate IP (optional)\nexport const tryGetRealIP = async () => {\n  try {\n    // This only works if the backend provides an endpoint to return client IP\n    const response = await fetch('/api/client-ip');\n    if (response.ok) {\n      const data = await response.json();\n      return data.ip || '127.0.0.1';\n    }\n  } catch (error) {\n    console.log('Could not get real IP, using localhost');\n  }\n  return '127.0.0.1';\n};\n\nexport default {\n  getClientInfo,\n  tryGetRealIP\n};