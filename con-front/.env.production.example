# Copy to .env.production for local prod builds, or set these in Netlify's dashboard.
# CRA embeds these into the JS bundle at build time.

# Point to your deployed Render backend URL (no trailing slash)
REACT_APP_API_URL=https://constructions-be.onrender.com

# Environment label (optional, used by the app in some places)
REACT_APP_ENV=production
