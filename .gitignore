# Old backup folders — not part of the current codebase
construction-frnted-old-bk2/
constructions-fe-old-bk/
playwright-tests/

# Postgres dumps (root-level only — database-construction/ is intentionally tracked)
/dump-postgres-*.sql
_dump_local.sql
*.dump
!database-construction/

# Claude Code metadata
.claude/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Node — belt & suspenders (subdir gitignores already cover this)
**/node_modules/
